# Example diagrams

One file per supported diagram type. Used for manual testing (they appear in the app's
**Examples** menu) and as a smoke test: `bun run examples` renders all of them with the CLI
into `.cli-cache/examples/` and fails if any of them doesn't render.
